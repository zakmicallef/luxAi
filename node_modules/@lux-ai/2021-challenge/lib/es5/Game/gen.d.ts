import { Game } from '.';
import { LuxMatchConfigs } from '../types';
export declare const generateGame: (matchconfigs?: Partial<LuxMatchConfigs>) => Game;
export interface GenerationConfigs {
    width: number;
    height: number;
    seed: number;
}
