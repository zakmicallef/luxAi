import { Design, Match, Tournament, MatchEngine, DesignOptions } from 'dimensions-ai';
import { LuxMatchResults } from './types';
import { DeepPartial } from 'dimensions-ai/lib/main/utils/DeepPartial';
export declare class LuxDesign extends Design {
    constructor(name: string, options?: DeepPartial<DesignOptions>);
    initialize(match: Match): Promise<void>;
    update(match: Match, commands: Array<MatchEngine.Command>): Promise<Match.Status>;
    getResults(match: Match): Promise<LuxMatchResults>;
    static resultHandler(results: LuxMatchResults): Tournament.RankSystem.Results;
}
